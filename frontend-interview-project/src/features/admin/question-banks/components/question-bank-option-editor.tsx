import { EmptyState } from "@/components/common/empty-state";

export function QuestionBankOptionEditor() {
  return (
    <EmptyState
      title="Option editor is ready"
      description="Connect this component to backend options when nested question answers are available."
    />
  );
}
